import db from "@db";
import { QueryResult } from "pg";

const deleteSql = "DELETE FROM ";
// export default async (roleId: number): Promise<any>=>{
//   const connection = await db.connect();
//   // const
// }
